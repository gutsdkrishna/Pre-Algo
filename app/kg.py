def apply_kg_bias(facet_scores, user_id=None):
    """
    Stub for applying knowledge-graph based personalization bias to facet scores.
    Currently returns input unmodified.
    """
    # facet_scores: list of (facet_id, score)
    # user_id: optional user identifier
    return facet_scores 